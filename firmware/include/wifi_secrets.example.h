#pragma once

// Copy this file to wifi_secrets.h and enter the credentials for the WLAN
// that should host the TONEX bridge. wifi_secrets.h is ignored by Git.
#define TONEX_WIFI_SSID "your-network-name"
#define TONEX_WIFI_PASS "your-network-password"
